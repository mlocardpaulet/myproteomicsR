% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replace_missing_values.R
\name{wrap_replace_missing_values}
\alias{wrap_replace_missing_values}
\title{Apply missing-value replacement to proteome and phosphoproteome tables}
\usage{
wrap_replace_missing_values(ltab, quantile_replacement_val = 0.01)
}
\arguments{
\item{ltab}{A named list with elements \code{proteome} and \code{ptm},
each being a list with \code{filtered_table} and \code{value_count}
elements as returned by \code{\link{filter_value_count}}.}

\item{quantile_replacement_val}{Numeric in \code{[0, 1]}. Quantile of the
per-run measured log2 values used as the replacement value.
Default: \code{0.01} (1st percentile).}
}
\value{
A named list with the same names as \code{ltab}, where each element
is the imputed long-format data frame returned by
\code{\link{replace_missing_values}}.
}
\description{
Iterates over the filtered proteome and phosphoproteome tables produced by
\code{\link{filter_value_count}} and calls \code{\link{replace_missing_values}}
on each, using the 1st percentile of measured values per run as the
imputation value.
}
\seealso{
\code{\link{replace_missing_values}}, \code{\link{filter_value_count}}
}
