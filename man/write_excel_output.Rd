% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_output.R
\name{write_excel_output}
\alias{write_excel_output}
\title{Write analysis results to a multi-sheet Excel workbook}
\usage{
write_excel_output(
  unfiltered_table_proteome,
  filtered_table_proteome,
  stat_results_proteome,
  mv_counts_proteome,
  proteome_after_replacement,
  unfiltered_table_phosphoproteome,
  filtered_table_phosphoproteome,
  stat_results_phosphoproteome,
  mv_counts_phosphoproteome,
  phosphoproteome_after_replacement,
  file_name = "output",
  path = "/"
)
}
\arguments{
\item{unfiltered_table_proteome}{A data frame. Raw (unfiltered) proteome
table.}

\item{filtered_table_proteome}{A data frame. Proteome table after quality
filtering, used as input to the statistical analysis.}

\item{stat_results_proteome}{A data frame. Limma output for the proteome
(as returned by \code{\link{run_limma}}).}

\item{mv_counts_proteome}{A data frame. Measured-value counts for the
proteome (as returned by \code{\link{missing_values_count}}).}

\item{proteome_after_replacement}{A data frame. Proteome table after missing
value imputation (as returned by \code{\link{replace_missing_values}}).}

\item{unfiltered_table_phosphoproteome}{A data frame. Raw (unfiltered)
phosphoproteome table.}

\item{filtered_table_phosphoproteome}{A data frame. Phosphoproteome table
after quality filtering.}

\item{stat_results_phosphoproteome}{A data frame. Limma output for the
phosphoproteome (as returned by \code{\link{run_limma}}).}

\item{mv_counts_phosphoproteome}{A data frame. Measured-value counts for
the phosphoproteome (as returned by \code{\link{missing_values_count}}).}

\item{phosphoproteome_after_replacement}{A data frame. Phosphoproteome table
after missing value imputation (as returned by
\code{\link{replace_missing_values}}).}

\item{file_name}{Character. Base name for the output file (without date
prefix or \code{.xlsx} extension). Default: \code{"output"}.}

\item{path}{Character. Directory path where the file should be saved.
Must end with \code{"/"}. Default: \code{"/"}.}
}
\value{
Invisibly returns \code{NULL}. Called for its side effect of writing
an Excel file to disk.
}
\description{
Assembles proteome and phosphoproteome analysis results into a named list
and writes each element as a separate sheet in a dated \code{.xlsx} file.
}
